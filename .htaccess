# Enable rewrites
RewriteEngine On

# Send all requests to index.php (optional, for clean URLs)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [L]
